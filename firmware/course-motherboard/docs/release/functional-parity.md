# 固件功能状态与验证清单

本分支是从完整公共底座中只移除 Host Action v1 的课程起点，不是过时或整套删减的教学固件。下表中的“源码完整”表示当前分支存在对应实现、构建接线和主机测试；它不等于真机功能已经验收。

| 功能区域 | 当前状态 | 当前实板与专项状态 |
| --- | --- | --- |
| ESP32-S3 / EasyInput V2.0 构建 | 源码完整；默认构建通过 | 当前公开候选已烧录并正常启动 |
| 8 个按键、编码器、输入反馈 | 源码完整；v2 主机测试通过 | 实板按键、旋转、按压与输入反馈正常 |
| USB HID 与 BLE HID | 源码完整；设备身份保持 `EasyInput AI`，协议和状态测试通过 | 实板连接、输入与重连主流程正常 |
| BLE 配置持久化 | 源码完整；三主机容量、CCCD 迁移与不静默淘汰绑定设备的策略测试通过 | 软件与构建已验证；不同主机升级迁移场景仍需独立实板观察 |
| 本地配置与状态 | 源码完整；USB、BLE 与兼容 Wi-Fi 配置路径保留 | 实板配置与状态主流程正常 |
| Host Action v1／打开应用 | 完整 `main` 已包含冻结协议、八键配置、USB／BLE 单通道路由和能力声明测试；课程 starter 故意不含实现 | 固件真实发送与兼容 App 的映射、同步和目标应用打开仍需端到端观察 |
| WS2812、状态灯与 GPIO8 共享电源 | 源码完整；电源仲裁测试和目标构建通过 | 实板灯光、状态反馈与音频并发主流程正常 |
| 电池、充电、睡眠与唤醒 | 源码完整；策略测试通过 | 实板供电、状态、睡眠与唤醒主流程正常 |
| 麦克风采集与 Wi-Fi 上行 | 源码完整；v1 控制 wire、会话和数据包测试齐备 | 在受信任网络配合兼容本地 companion 的端到端主流程正常 |
| 扬声器输出与播放 | 源码完整；播放和服务启动测试通过 | 实板启动声音与播放主流程正常 |
| 声音格式、读取与 A/B 存储 | 源码完整；资源、存储和读取测试通过 | 当前资源读取与播放正常；破坏性掉电边界仍由自动测试和专项测试分别验证 |
| 声音同步、会话与网络传输 | 源码完整；协议、会话、运行时和 Wi-Fi wire 测试通过 | 当前网络同步主流程正常；异常网络边界继续由协议与专项测试覆盖 |
| WaytoAGI 出厂提示音 | 固定 EIAD 资源和应用内回退播放路径齐备 | 冷启动播放与听感正常 |
| IMA-ADPCM 诊断 | 独立项目自有测试夹具与主机解码测试齐备 | ESP-IDF 构建、真机听感与内存水位 |
| Ogg/Opus 诊断 | 源码、固定依赖和独立项目自有测试夹具齐备 | ESP-IDF 构建、真机解码播放与内存水位 |

## 安全与配置边界

- 麦克风控制 wire 当前为 v1，只限制来源 IP 必须与已配置 `audio_host` 的解析地址一致；预留 token 不做密码学验证，也不防重放。
- 同一已配置主机地址可以向 Wi-Fi 控制端口发送配置 JSON；该能力只适合受信任本地网络，不应暴露到互联网或不可信局域网。
- Wi-Fi 凭据与同步密钥可以经既有配置通道提供；仓库不保存任何真实值。
- 仓库不保存真实凭据、设备标识或部署配置；公开测试中的固定值只是测试向量。
- 固件仓库不包含桌面 companion 应用，因此麦克风上行的端到端体验需要另行连接兼容实现。

## 发布判据

当前默认固件已在 EasyInput V2.0 实板完成联合功能测试，主流程观察正常。该结论对应当前公开候选和当前实板，不替代 IMA-ADPCM、Ogg/Opus 等独立诊断构建，也不自动扩展为所有电脑、网络环境、硬件批次和破坏性故障场景均已验证。
